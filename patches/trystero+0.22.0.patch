diff --git a/node_modules/trystero/src/peer.js b/node_modules/trystero/src/peer.js
index 791fcec..7118736 100644
--- a/node_modules/trystero/src/peer.js
+++ b/node_modules/trystero/src/peer.js
@@ -1,6 +1,6 @@
 import {all, alloc} from './utils.js'
 
-const iceTimeout = 5000
+const iceTimeout = 2000
 const iceStateEvent = 'icegatheringstatechange'
 const offerType = 'offer'
 const answerType = 'answer'
diff --git a/node_modules/trystero/src/strategy.js b/node_modules/trystero/src/strategy.js
index 965356e..13e88b8 100644
--- a/node_modules/trystero/src/strategy.js
+++ b/node_modules/trystero/src/strategy.js
@@ -14,8 +14,8 @@ import {
   watchOnline
 } from './utils.js'
 
-const poolSize = 20
-const announceIntervalMs = 5_333
+const poolSize = 2
+const announceIntervalMs = 3_000
 const offerTtl = 57_333
 
 export default ({init, subscribe, announce}) => {
@@ -286,6 +286,12 @@ export default ({init, subscribe, announce}) => {
         delete occupiedRooms[appId][roomId]
         announceTimeouts.forEach(clearTimeout)
         unsubFns.forEach(async f => (await f)())
+        // Destroy pooled offers to release TURN allocations — without this,
+        // each room creation leaks poolSize RTCPeerConnections that hold
+        // relay allocations open, eventually exhausting TURN server limits.
+        offerPool.forEach(peer => peer.destroy())
+        offerPool = []
+        Object.values(pendingOffers).flat().forEach(peer => peer?.destroy())
         clearInterval(offerCleanupTimer)
         cleanupWatchOnline()
         didInit = false
diff --git a/node_modules/trystero/src/supabase.js b/node_modules/trystero/src/supabase.js
index d35a8e5..62c4efd 100644
--- a/node_modules/trystero/src/supabase.js
+++ b/node_modules/trystero/src/supabase.js
@@ -51,26 +51,49 @@ export const joinRoom = strategy({
       )
     )
 
-    subscribe(rootTopic, chan =>
+    subscribe(rootTopic, chan => {
+      // Store the subscribed root channel on the client so announce() can
+      // reuse it instead of creating a new (unsubscribed) channel each time.
+      client.__trysteroRootChan = chan
       chan.on(events.broadcast, {event: events.join}, ({payload}) =>
         onMessage(rootTopic, payload, handleMessage)
       )
-    )
+      // Announce immediately when channel is ready — the first scheduled
+      // announce fires before subscription completes and silently fails,
+      // so this eliminates the 0-8s dead zone for peer discovery.
+      chan.send({
+        type: events.broadcast,
+        event: events.join,
+        payload: {peerId: selfId}
+      })
+    })
 
     let didUnsub = false
 
     return () => {
-      allChans.forEach(chan => client.removeChannel(chan))
       didUnsub = true
+      allChans.forEach(chan => client.removeChannel(chan))
+      client.__trysteroRootChan = null
+      // Disconnect the realtime WebSocket — without this, subsequent
+      // joinRoom() calls create new clients while old ones leak connections,
+      // breaking peer discovery on the second game.
+      client.removeAllChannels()
     }
   },
 
-  announce: (client, rootTopic) =>
-    client.channel(rootTopic).send({
-      type: events.broadcast,
-      event: events.join,
-      payload: {peerId: selfId}
-    })
+  announce: (client, rootTopic) => {
+    // Use the already-subscribed root channel stored during subscribe().
+    // Creating a new channel via client.channel() returns an unsubscribed
+    // instance whose send() silently fails, breaking peer discovery.
+    const chan = client.__trysteroRootChan
+    if (chan) {
+      chan.send({
+        type: events.broadcast,
+        event: events.join,
+        payload: {peerId: selfId}
+      })
+    }
+  }
 })
 
 export {selfId} from './utils.js'
